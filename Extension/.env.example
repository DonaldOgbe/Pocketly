# Base URL of the Pocketly API. Must also appear in host_permissions,
# which vite.config.ts injects into the manifest at build time.
VITE_API_BASE_URL="http://localhost:3000"

# Where "Open Pocketly" sends the user
VITE_WEB_APP_URL="http://localhost:5173"
